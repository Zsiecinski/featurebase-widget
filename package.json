{
  "name": "featurebase-intercom",
  "version": "0.1.0",
  "private": true,
  "description": "Intercom Canvas Kit Messenger app that surfaces the Featurebase 'Done' roadmap column.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node scripts/db-migrate.mjs && node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/",
    "logos": "node scripts/generate-pngs.mjs",
    "db:migrate": "node scripts/db-migrate.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
